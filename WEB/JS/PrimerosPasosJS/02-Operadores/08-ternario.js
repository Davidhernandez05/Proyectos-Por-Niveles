/**
 * Expresion ? 'Si es true' : 'Si es false';
 * 
 * Es muy parecido al IF solo que para tomar decisiones mas abreviadas:
 */

let edad = 16;
let acceso = edad >= 18 ? 'Tiene acceso' : 'No tiene acceso';

console.log(acceso);